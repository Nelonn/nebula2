## `cert`

This is a library for interacting with `nebula` style certificates and authorities.

Only v3 `nebula` certificates are supported.

## v3

This version uses ASN.1 DER encoding and carries the HPKE public key used by
the v3 handshake.
